import { createStyles } from '@mantine/styles';

export const TabPanelStyles = createStyles((theme, { active }, getRef) => {
  return {
    root: {
      width: '100%',
      flex: 'none',
    },
  };
});
