import { createStyles } from '@mantine/styles';

export const TabPaneStyles = createStyles((theme, { active }, getRef) => {
  return {
    root: {
      width: '100%',
      flex: 'none',
    },
  };
});
