import { createStyles } from '@mantine/styles';
import { pxToRem, getPaddings, getFontExpressive, getFontProductive } from '../../theme.mixins';

export const ActivityAnswersBarStyles = createStyles((theme, {}) => {
  return {
    root: {
      height: '100%',
      width: '100%',
    },
  };
});
