import { createStyles } from '@mantine/styles';

export const SortableListStyles = createStyles((theme, {}) => {
  return {
    item: {
      display: 'flex',
      alignItems: 'center',
    },
  };
});
