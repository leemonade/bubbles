import PropTypes from 'prop-types';

export const MODAL_ZOOM_DEFAULT_PROPS = {
  canPlay: false,
  style: {},
};
export const MODAL_ZOOM_PROP_TYPES = {
  children: PropTypes.node,
  canPlay: PropTypes.bool,
  style: PropTypes.object,
};
