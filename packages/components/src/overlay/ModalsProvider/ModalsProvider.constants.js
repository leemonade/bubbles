import PropTypes from 'prop-types';

export const MODALS_PROVIDER_DEFAULT_PROPS = {};
export const MODALS_PROVIDER_PROP_TYPES = {
  modals: PropTypes.object,
  children: PropTypes.element,
  modalProps: PropTypes.object,
};
