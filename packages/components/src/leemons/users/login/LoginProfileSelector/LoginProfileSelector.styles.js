import { createStyles } from '@mantine/styles';
import {
  pxToRem,
  getPaddings,
  getFontExpressive,
  getFontProductive,
} from '../../../../theme.mixins';

export const LoginProfileSelectorStyles = createStyles((theme, {}) => {
  return {
    root: {},
    title: {},
    description: {},
    radioGroup: {},
    checkBox: {},
    lowerHelp: {
      margin: 0,
    },
  };
});
