import { createStyles } from '@mantine/styles';

const TagsInputStyles = createStyles((theme, {}) => ({
  root: {},
  tagsContainer: {
    marginTop: theme.spacing[4],
  },
}));

export { TagsInputStyles };
