import PropTypes from 'prop-types';

export const SLIDER_DEFAULT_PROPS = {
  label: null,
  thumbSize: 16,
};
export const SLIDER_PROP_TYPES = {
  label: PropTypes.string,
  thumbSize: PropTypes.number,
};
