import { Story, Props, Source, Preview } from '@storybook/addon-docs';
import { Group, Box } from '@mantine/core';
import { DotsHorizontalIcon, ExternalLinkIcon, ChevronRightIcon } from '@heroicons/react/outline';
import { ThemeProvider } from './../../ThemeProvider';
import * as stories from './ActionButton.stories.js';
import { ActionButton } from './ActionButton.js';

# ActionButton

[Source code](https://github.com/leemonade/bubbles/tree/main/packages/components/src/form/ActionButton/ActionButton.js)

## Table of Contents

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [Overview](#overview)
- [Options](#options)
- [Variations](#variations)
- [Feedback](#feedback)

## Overview

We use these buttons for similar task-based actions within a workflow built into the interface so as not to attract too much attention.

## Options

ActionButtons can have only the icon or also include a label with descriptive text to accompany it, through the `label` property. **When we only want the icon, we must also indicate a descriptive text** through the `description` property that will be displayed in a tooltip when hovering over the button:

<ThemeProvider>
  <Group style={{ gap: 32 }} mt="24px" mb="44px" mx="2px">
    <Box>
      <ActionButton size="sm" tooltip="Expand" icon={<DotsHorizontalIcon />} />
    </Box>
    <Box>
      <ActionButton size="sm" title="Expand" icon={<DotsHorizontalIcon />}>
        Expand
      </ActionButton>
    </Box>
  </Group>
</ThemeProvider>

## Variations

We have two sizes xs and sm, by default we will use the size sm. There are apositive and negative version of color depending on the theme and the background in which they are placed

<ThemeProvider>
  <Group style={{ gap: 32 }} mt="24px" mx="2px">
    <Box>
      <ActionButton size="sm" tooltip="Expand" icon={<DotsHorizontalIcon />} />
    </Box>
    <Box>
      <ActionButton size="sm" title="Expand" icon={<DotsHorizontalIcon />}>
        Expand
      </ActionButton>
    </Box>
    <Box>
      <ActionButton size="xs" tooltip="Expand" icon={<DotsHorizontalIcon />} />
    </Box>
    <Box>
      <ActionButton size="xs" title="Expand" icon={<DotsHorizontalIcon />}>
        Expand
      </ActionButton>
    </Box>
  </Group>

  <Group style={{ gap: 32 }} mb="44px" mx="2px">
    <Box>
      <ActionButton size="sm" color="negative" tooltip="Expand" icon={<DotsHorizontalIcon />} />
    </Box>
    <Box>
      <ActionButton size="sm" color="negative" title="Expand" icon={<DotsHorizontalIcon />}>
        Expand
      </ActionButton>
    </Box>
    <Box>
      <ActionButton size="xs" color="negative" tooltip="Expand" icon={<DotsHorizontalIcon />} />
    </Box>
    <Box>
      <ActionButton size="xs" color="negative" title="Expand" icon={<DotsHorizontalIcon />}>
        Expand
      </ActionButton>
    </Box>
  </Group>
</ThemeProvider>

## Component API

<Preview>
  <Story id="atoms-form-actionbutton--playground" />
</Preview>

<Props of={ActionButton} />

## Feedback

Help us improve this component by providing feedback, asking questions on Slack, or updating this file on
[GitHub](https://github.com/leemonade/bubbles/edit/main/packages/components/form/ActionButton.mdx)
