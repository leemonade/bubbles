import { createStyles } from '@bubbles-ui/components';

export const LinkModalStyles = createStyles((theme, {}) => {
  return {
    root: {},
    buttonRow: {},
    radioGroup: { marginBottom: 1 },
    form: {
      padding: 16,
      paddingBottom: 18,
    },
    switch: {
      marginLeft: -8,
    },
  };
});
