import { createStyles } from '@bubbles-ui/components';

export const ButtonGroupStyles = createStyles((theme, {}) => {
  return {
    root: {
      display: 'inline-flex',
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'start',
    },
  };
});
