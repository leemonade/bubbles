import { createStyles, pxToRem } from '@bubbles-ui/components';

export const BubbleMenuStyles = createStyles((theme, {}) => {
  return {
    root: {
      display: 'flex',
      alignItems: 'center',
    },
  };
});
