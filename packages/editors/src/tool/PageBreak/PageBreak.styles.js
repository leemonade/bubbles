import { createStyles } from '@bubbles-ui/components';

export const PageBreakStyles = createStyles((theme) => ({
  pageBreak: {
    display: 'block',
    marginBottom: '20px',
    borderBottom: '2px dashed #ccc',
    pageBreakBfter: 'always',
  },
}));
