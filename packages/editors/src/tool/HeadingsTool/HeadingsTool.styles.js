import { createStyles } from '@bubbles-ui/components';

export const HeadingsToolStyles = createStyles((theme) => {
  return {
    root: {
      width: '100%',
      maxWidth: 170,
      display: 'flex',
      flexDirection: 'column',
      gap: 4,
    },
  };
});
